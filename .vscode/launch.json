{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "TestDecoder",
            "type": "go",
            "request": "launch",
            "mode": "test",
            "program": "${workspaceRoot}",
            "args": [
                "-test.v",
                "-test.run",
                "TestDecoder"
            ]
        }
    ]
}